{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "./SentinelServer.x86_64 --headless -- --server --port=${PORT}",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
