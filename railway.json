{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "./builds/server/SentinelServer.x86_64 --headless -- --server --port=${PORT}",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
